# vector-add-openacc 

Parallel vector addition with OpenACC this time.  Just a pragma to initialize the number of gangs (thread blocks), and then another pragma to automatically allocate work items per gang.
